{
  "name": "@optcgdb/data",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/data",
  "projectType": "library",
  "targets": {
    "scrape": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "apps/data",
        "commands": ["vite-node ./utils/scraper.ts"]
      }
    },
    "watch": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "apps/data",
        "commands": ["rm -rf dist", "tsc -w"]
      }
    },
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "apps/data",
        "commands": ["rm -rf dist", "tsc"]
      }
    }
  }
}
